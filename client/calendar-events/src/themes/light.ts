import { interfaces } from "@/@types/interfaces";

const light: interfaces.ThemeColors = {
  primary: "rgb(30,130,230)",
  layout: "white",
  secondary: "rgb(230,60,90)",
  smooth: "rgba(0,0,0,.3)",
};

export default light;
